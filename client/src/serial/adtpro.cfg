MEMORY {
    RAM: start = $0803, size = $8000, define = yes, file = %O;
}

SEGMENTS {
    STARTUP: load = RAM, type = ro;
    CODE: load = RAM, type = ro;
    FORMAT: load = RAM, type = rw;
    DATA: load = RAM, type = rw;
    RODATA: load = RAM, type = ro;
    BSS: load = RAM, type = bss;
}
